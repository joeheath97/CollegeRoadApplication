@model IEnumerable<CollegeRoadApplication.Models.SwimmingEvent>

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(m => m.Name)</th>
            <th>@Html.DisplayNameFor(m => m.Distance)</th>
            <th>@Html.DisplayNameFor(m => m.AgeRange)</th>
            <th>@Html.DisplayNameFor(m => m.Gender)</th>
            <td></td>
        </tr>
    </thead>
    <tbody>
        @if (Model.Count() != 0)
        {
            foreach (var race in Model)
            {
        <tr>
            <td>@Html.ActionLink(race.Name, "Edit", "SwimmingEvent", new { id = race.Id }, null)</td>
            <td>@race.Distance</td>
            <td>@race.AgeRange</td>
            <td>@race.Gender</td>
            <td>@Html.ActionLink("Delete", "Delete", "SwimmingEvent", new { id = race.Id }, null)</td>
        </tr>
            }
        }
        else
        {
            <tr><td colspan="4" class="text-center">No Results Found</td></tr>
        }

    </tbody>
</table>
